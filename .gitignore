node_modules
dist
.env
.env.local
*.log
.vitest-cache

# spikes have their own node_modules
spikes/node_modules

# Pre-Plan-5 space-agent remnants — kept ignored in case anyone still has
# a local clone on disk; safe to delete the directories outright.
vendor/
customware/

# Vite build output
app/dist/
.vite/

# Electron-builder output + bundled backend
release/
dist-backend/

# Per-user OpenCanvas cache + DB (when devving from inside a clone of the repo)
.opencanvas/

# Test environment artifacts
__tests__/app/.cache/

# demo recorder intermediate frames (the produced docs/demo.gif IS committed)
docs/demo-frames/
